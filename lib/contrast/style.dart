import 'package:flutter/material.dart';

TextStyle myTextStyle (BuildContext context) => TextStyle(
    fontWeight: FontWeight.bold,
    color: Theme.of(context).colorScheme.primary,
    wordSpacing: 1.25
);